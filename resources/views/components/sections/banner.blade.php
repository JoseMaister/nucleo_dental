<div class="bg-indigo-700 text-white text-center p-6 mt-auto">
    <div class="max-w-7xl mx-auto">
        <p class="text-3xl md:text-4xl">
            {!! __('messages.banner.title', ['today' => '<span class="font-bold">'.__('messages.banner.today').'</span>']) !!}
        </p>
        <small class="block mt-2">
            {!! __('messages.banner.location', ['city' => __('messages.banner.city')]) !!}
        </small>
    </div>
</div>